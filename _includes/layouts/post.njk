---
layout: layouts/base.njk
templateClass: tmpl-post
---

{% set words = content | striptags | wordcount %}
{% set wc = words / 240 %}

<article class="max-w-4xl mx-auto mt-8">
    <header class="mb-8">
        <h1>{{ title }}</h1>
        <div class="flex text-sm space-x-4 items-center">
            <div class="flex items-center space-x-2">
                <span class="fill-current">{% icon 'calendar' %}</span>
                <span>{{ page.date | readableDate }}</span>
            </div>
        </div>
        <div>{% include 'tags.njk' %}</div>
        <aside class="text-sm text-gray-900">{{wc | round}} minute read</aside>
    </header>
    <section>
        {{ content | safe }}
    </section>
</article>
{% if prevPost and not prevPost.data.external %}
    <div class="flex justify-center mt-8">
        <a class="flex items-center font-medium space-x-4" href="{{ prevPost.url | url }}">
            {% icon 'arrow-narrow-left' %}
            <span>{{ prevPost.data.title}}</span>
        </a>
    </div>
{% endif %}

