<!doctype html>
<html lang="en">
    {% include "head.njk" %}
    <body>
        {%- if draft %}
        <div class="w-full font-bold text-center text-white bg-yellow-500 select-none">DRAFT</div>
        {% endif %}
        {% include "header.njk" %}
        <main class="container mx-auto px-8 space-y-8 {{ templateClass }}">
            {{ content | safe }}
        </main>
        {% include "footer.njk" %}
    </body>
</html>
