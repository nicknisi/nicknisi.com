<!doctype html>
<html lang="en">
    {% include "head.njk" %}
    <body>
        {%- if draft %}
        <div class="w-full font-bold text-center text-white bg-yellow-500 select-none">DRAFT</div>
        {% endif %}
        {% include "header.njk" %}
        <main class="px-6 mb-auto">
          <div class="max-w-2xl mx-auto">
            {{ content | safe }}
          </div>
        </main>
        {% include "footer.njk" %}
    </body>
</html>
