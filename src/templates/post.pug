extends ../layouts/default.pug

block coverPhoto
	- var coverPhotoUrl = page.cover

block content
	section
		article
			h1 #{page && page.title}
			div.sub
				span.date #{page && page.dateString}
			!= passthrough(contents)
