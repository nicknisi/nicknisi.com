---
import Base from './Base.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<Base title={title}>
	<div class="py-10">
		<h1 class="mb-6 text-4xl font-extrabold md:text-5xl">{title}</h1>
		<slot />
	</div>
</Base>
