---
interface Props { totalTokens:number; }
const {totalTokens}=Astro.props;
const ratio=Math.round(totalTokens/215_000);
---
<p class="mt-16 border-t border-line pt-6 text-center text-sm text-ink-faint">You’ve used ~{ratio.toLocaleString()}× more tokens than Moby-Dick.</p>
